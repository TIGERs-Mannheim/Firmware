/*
 * bits.h
 *
 *  Created on: 28.10.2015
 *      Author: AndreR
 */

#ifndef BITS_H_
#define BITS_H_

#define BITS_CLEAR(reg, bit) (reg &= ~(bit))
#define BITS_SET(reg, bit) (reg |= (bit))

#endif /* BITS_H_ */
